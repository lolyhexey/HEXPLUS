VERSAO: 53

@lolyhexey

proxy.py / wsproxy.py / open.py optimize latency: TCP_NODELAY + TCP_KEEPALIVE + uvloop
ตัด asyncio.wait_for ออกจาก pipe loop (ลด overhead per-read) — SSH ไม่หน่วง 200ms อีก
ทุก proxy รับ DEFAULT_HOST + STATUS_CODE + STATUS_MSG จาก argv ปรับแต่งได้เหมือนกันหมด
SOCKS SSH / SOCKS OPENVPN ตอน start มี prompt ครบเหมือน WEBSOCKET
SOCKS OPENVPN DEFAULT HOST auto-detect จาก /etc/openvpn/server.conf
Plus ติดตั้ง uvloop + แก้ python-pip เป็น python3-pip (รองรับ Ubuntu 22+)
แปล PORTAS เป็นไทย → พอร์ตที่เปิด
RESPONSE MSG ใส่ headers เพิ่มได้ผ่าน \r\n (เช่น Content-length: 0)
default ของ code 200/400/520 มี Content-length: 0 ติดมาด้วยแล้ว
ระบบ update script เขียนใหม่: guard figlet, โหลด Plus ลง /root, ตรวจไฟล์จริงไม่ใช่ 404
release notes ตอน attscript ตอนนี้ข้าม VERSAO/author/footer โชว์เฉพาะรายการแก้
แสดงเวอร์ชันเก่า → ใหม่ ตอนพบอัปเดต (v52 → v53)
แก้บั๊ก PORTAS โชว์ args ของ wsproxy.py เกินมา + แก้ xargs unmatched quote
wsproxy.py เลือก STATUS CODE ได้ตอน start: 101/200/400/520/custom พร้อม default msg ของแต่ละ code
wsproxy.py ถาม DEFAULT HOST + RESPONSE MSG ตอน start ปรับแต่งได้ + มี default ให้
เมนู PROXY SOCKS เพิ่มบรรทัด PORTAS สรุปด้านบน + เก็บ (พอร์ต: ...) inline ของเดิมไว้ด้วย
SOCKS OPENVPN (open.py) อ่าน OpenVPN port จาก server.conf เอง ไม่ hardcode 1194 แล้ว
แก้ criarusuario newclient ใช้ชื่อไฟล์ cert ผิด (ca.pem→ca.crt) + ใส่ server cert แทน client cert
แก้ criarteste ห้ามชื่อขึ้นต้นด้วยตัวเลข + error capture stderr
ลบ comp-lzo yes ออกจาก OpenVPN config (แตก OpenVPN 2.6 / Ubuntu 24)
เพิ่ม data-ciphers ใน OpenVPN รองรับ negotiation cipher ใหม่
แก้ chisel username ห้ามขึ้นต้นด้วยตัวเลขหรือเครื่องหมาย
ย้าย instsqd จาก apt-key (deprecated) มาใช้ signed-by keyring รองรับ Ubuntu 24
แก้ createuser ไม่ให้ยอมรับชื่อขึ้นต้นด้วยตัวเลข (useradd ปฏิเสธ)
error createuser ตอนนี้โชว์ข้อความจริงจาก useradd + path log
แก้ OpenVPN เริ่มไม่ได้บน Ubuntu 22+: client-cert-not-required ถูกถอด ใช้ verify-client-cert none แทน
แก้ openvpn --genkey ให้ใช้ syntax ใหม่ก่อน fallback ไปของเก่า (รองรับ OpenVPN 2.6)
เพิ่ม log ตอนติดตั้ง: /var/log/hexplus-install.log
ข้อความ error ตอนนี้บอก path log + คำสั่งดู error ทันที
แก้บั๊ก figlet หาไม่เจอตอนเริ่มต้น Plus
ปิดเสียง output ที่หลุดมาตอนติดตั้งแพ็กเกจ
แก้ permission ของ /var/log/squid3 ให้ proxy user
เพิ่มไฟล์ EasyRSA-3.0.1.tgz ที่หายไป (OpenVPN ติดตั้งไม่ได้)
แก้ typo apt-get update-y

--[RECUSE IMITAÇŌES]--
